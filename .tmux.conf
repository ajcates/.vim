# Enable mouse support so tmux panes can be scrolled normally over SSH.
set -g mouse on

# Keep a larger scrollback buffer for remote sessions.
set -g history-limit 50000

# Use vi-style keys in copy mode.
setw -g mode-keys vi

# Preserve alternate screen behavior for full-screen apps over SSH.
setw -g alternate-screen on
